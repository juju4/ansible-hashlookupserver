---
galaxy_info:
  author: juju4
  role_name: hashlookupserver
  description: Install hashlookup-server, minimal and fast open source server (ReST/API) to lookup quickly hash value from large dataset.
  license: BSD
  min_ansible_version: '2.1'
  platforms:
    - name: EL
      versions:
        - all
    - name: Ubuntu
      versions:
        - focal
  galaxy_tags:
    - security
    - forensics
dependencies:
  - role: juju4.kvrocks
